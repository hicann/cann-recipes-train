diff --git a/verl/utils/device.py b/verl/utils/device.py
index e0e917a..f602927 100644
--- a/verl/utils/device.py
+++ b/verl/utils/device.py
@@ -144,6 +144,8 @@ def set_expandable_segments(enable: bool) -> None:
     """
     if is_cuda_available:
         torch.cuda.memory._set_allocator_settings(f"expandable_segments:{enable}")
+    if is_npu_available:
+        torch.npu.memory._set_allocator_settings(f"expandable_segments:{enable}")


 def auto_set_device(config) -> None:
@@ -193,8 +195,30 @@ def get_npu_versions() -> tuple[str, str]:
     Raises:
         RuntimeError: If unable to retrieve version information
     """
-    # Check npu-smi software version
-    result = subprocess.run(["npu-smi", "info", "-t", "board", "-i", "1"], capture_output=True, text=True, check=True)
+    # Use board IDs reported by npu-smi, not logical device or die IDs.
+    listing = subprocess.run(["npu-smi", "info", "-l"], capture_output=True, text=True, check=True)
+    board_ids = []
+    for line in listing.stdout.splitlines():
+        name, _, value = line.partition(":")
+        if name.strip() == "NPU ID" and value.strip().isdigit():
+            board_ids.append(value.strip())
+    if not board_ids:
+        raise RuntimeError("No NPU board IDs found in npu-smi info -l output")
+
+    last_error = None
+    for board_id in board_ids:
+        try:
+            result = subprocess.run(
+                ["npu-smi", "info", "-t", "board", "-i", board_id],
+                capture_output=True,
+                text=True,
+                check=True,
+            )
+            break
+        except subprocess.CalledProcessError as exc:
+            last_error = exc
+    else:
+        raise RuntimeError(f"Unable to query any NPU board from {board_ids}") from last_error

     # Parse software version from output
     software_version = None
diff --git a/verl/utils/vllm/npu_vllm_patch.py b/verl/utils/vllm/npu_vllm_patch.py
index 727afc8..ccfc080 100644
--- a/verl/utils/vllm/npu_vllm_patch.py
+++ b/verl/utils/vllm/npu_vllm_patch.py
@@ -169,7 +169,7 @@ def patch_vllm013_rotary_emb():
         is_neox_style: bool = True,
         enable_fp32_compute: bool = False,
     ) -> None:
-        super(ApplyRotaryEmb, self).__init__(enforce_enable)
+        super(ApplyRotaryEmb, self).__init__(enforce_enable=enforce_enable)
         self.is_neox_style = is_neox_style
         self.enable_fp32_compute = enable_fp32_compute
         self.apply_rotary_emb_flash_attn = None
diff --git a/verl/workers/engine/torchtitan/transformer_impl.py b/verl/workers/engine/torchtitan/transformer_impl.py
index 739ccf2..dc463c7 100644
--- a/verl/workers/engine/torchtitan/transformer_impl.py
+++ b/verl/workers/engine/torchtitan/transformer_impl.py
@@ -29,6 +29,14 @@ from tensordict import TensorDict
 from torch.distributed.checkpoint.state_dict import get_model_state_dict
 from torch.distributed.tensor import DTensor
 from torchtitan.components.checkpoint import CheckpointManager
+try:
+    import torchtitan_npu
+except ImportError as exc:
+    raise ImportError(
+        "torchtitan_npu is required for the Qwen3 TorchTitan NPU recipe. "
+        "Run setup_backend.sh before starting training."
+    ) from exc
+
 from torchtitan.components.lr_scheduler import LRSchedulersContainer
 from torchtitan.components.optimizer import OptimizersContainer
 from torchtitan.config import CompileConfig, ParallelismConfig, TrainingConfig
@@ -49,6 +57,7 @@ from verl.utils.fsdp_utils import (
     offload_fsdp_model_to_cpu,
     offload_fsdp_optimizer,
 )
+from verl.utils.memory_utils import aggressive_empty_cache
 from verl.utils.model import extract_multi_modal_inputs
 from verl.utils.torch_functional import logprobs_from_logits
 from verl.workers.config import HFModelConfig, TorchtitanEngineConfig, TorchtitanOptimizerConfig
@@ -69,6 +78,18 @@ logger.setLevel(os.getenv("VERL_LOGGING_LEVEL", "WARN"))
 device_name = get_device_name()


+def apply_npu_patcher(config: Trainer.Config):
+    from torchtitan.protocols.model_converter import ModelConvertersContainer
+    from torchtitan_npu.converters.registry import get_model_converter_config
+    config.model_converters = ModelConvertersContainer.Config(
+        converters=[
+            get_model_converter_config("npu_rms_norm"),
+            get_model_converter_config("npu_rope"),
+        ],
+    )
+    config.activation_checkpoint.mode = "full"
+
+
 class TorchTitanEngine(BaseEngine):
     """
     Concrete Engine implementation using PyTorch TorchTitan parallelism.
@@ -106,8 +127,15 @@ class TorchTitanEngine(BaseEngine):
         torchtitan_name, torchtitan_flavor = derive_torchtitan_name_and_flavor(self.model_config.hf_config)

         # Get ModelSpec from model registry
-        model_module = importlib.import_module(f"torchtitan.models.{torchtitan_name}")
+        if torchtitan_name == "qwen3":
+            model_module = importlib.import_module("torchtitan_npu.models.qwen3")
+        else:
+            model_module = importlib.import_module(f"torchtitan.models.{torchtitan_name}")
         model_spec = model_module.model_registry(torchtitan_flavor)
+        if self.engine_config.attn_type == "varlen" and torchtitan_name == "qwen3":
+            from torchtitan_npu.models.qwen3.tnd_config import _enable_npu_varlen_attention
+
+            model_spec = _enable_npu_varlen_attention(model_spec)

         # Override attn_backend on the model config if needed
         attn_type = self.engine_config.attn_type
@@ -148,9 +176,12 @@ class TorchTitanEngine(BaseEngine):
             initial_load_in_hf=True,
             initial_load_model_only=True,
             initial_load_path=model_config.path,
+            last_save_model_only=False,
         )
         compile_config = CompileConfig(enable=self.engine_config.use_torch_compile)
         training_kwargs = {}
+        if not self.engine_config.forward_only:
+            training_kwargs["steps"] = total_steps
         if self.engine_config.max_seq_len is not None:
             training_kwargs["seq_len"] = self.engine_config.max_seq_len
         if self.engine_config.offload_policy or self.engine_config.forward_only:
@@ -171,6 +200,7 @@ class TorchTitanEngine(BaseEngine):
             # Use a no-op dataloader since verl has its own data loading
             dataloader=NoOpDataLoader.Config(),
         )
+        apply_npu_patcher(self.config)
         self.trainer = Trainer(self.config)

         self._init_device_mesh()
@@ -418,17 +448,63 @@ class TorchTitanEngine(BaseEngine):
                 for module in self.module:
                     load_fsdp_model_to_gpu(module)
             if optimizer and self.optimizer is not None:
-                load_fsdp_optimizer(self.optimizer, device)
+                for optimizer_instance in self.optimizer.optimizers:
+                    load_fsdp_optimizer(optimizer_instance, device)
             gc.collect()
         elif device == "cpu":
             if model:
                 for module in self.module:
-                    offload_fsdp_model_to_cpu(module)
+                    offload_fsdp_model_to_cpu(module, empty_cache=False)
+                self._invalidate_checkpoint_cache()
             if optimizer and self.optimizer is not None:
-                offload_fsdp_optimizer(self.optimizer)
+                for optimizer_instance in self.optimizer.optimizers:
+                    offload_fsdp_optimizer(optimizer_instance)
+            # Optimizer state copies are non-blocking. Synchronize only after
+            # every model shard and leaf optimizer has moved, then release the
+            # allocator cache at the phase boundary shared with colocated vLLM.
+            aggressive_empty_cache(force_sync=True)
+            if model:
+                self._assert_checkpoint_cache_released()
         else:
             raise ValueError(f"Invalid device type: {device}")

+    def _checkpoint_model_wrapper(self) -> Any:
+        try:
+            wrapper = self.checkpointer.states["model"]
+        except (AttributeError, KeyError, TypeError) as exc:
+            raise RuntimeError(
+                "TorchTitan checkpointer does not expose states['model']"
+            ) from exc
+
+        if not hasattr(wrapper, "cache_state_dict"):
+            raise RuntimeError(
+                "TorchTitan model wrapper does not expose cache_state_dict"
+            )
+        return wrapper
+
+    def _invalidate_checkpoint_cache(self) -> None:
+        """Release cached model tensor references after actor parameter offload."""
+        self._checkpoint_model_wrapper().cache_state_dict = {}
+
+    def _refresh_checkpoint_cache(self) -> None:
+        """Rebuild checkpoint state from the current model immediately before I/O."""
+        wrapper = self._checkpoint_model_wrapper()
+        get_state_dict = getattr(wrapper, "_get_state_dict", None)
+        if not callable(get_state_dict):
+            raise RuntimeError(
+                "TorchTitan model wrapper does not expose _get_state_dict()"
+            )
+        wrapper.cache_state_dict = get_state_dict()
+
+    def _assert_checkpoint_cache_released(self) -> None:
+        """Fail fast when checkpoint state still owns actor tensor references."""
+        cache_state_dict = self._checkpoint_model_wrapper().cache_state_dict
+        if cache_state_dict:
+            raise RuntimeError(
+                "TorchTitan checkpoint model cache still retains "
+                f"{len(cache_state_dict)} entries after actor offload"
+            )
+
     def save_checkpoint(
         self,
         local_path: str,
@@ -449,12 +525,19 @@ class TorchTitanEngine(BaseEngine):
         if max_ckpt_to_keep is not None:
             self.checkpointer.keep_latest_k = max_ckpt_to_keep

-        self.checkpointer.save(curr_step=global_step)
+        if self._is_offload_param:
+            self._refresh_checkpoint_cache()
+
+        # verl has already selected this step according to trainer.save_freq.
+        self.checkpointer.save(curr_step=global_step, last_step=True)

         torch.distributed.barrier()
         if self._is_offload_param:
             for module in self.module:
-                offload_fsdp_model_to_cpu(module)
+                offload_fsdp_model_to_cpu(module, empty_cache=False)
+            self._invalidate_checkpoint_cache()
+            aggressive_empty_cache(force_sync=True)
+            self._assert_checkpoint_cache_released()

     def load_checkpoint(
         self, local_path: str, hdfs_path: Optional[str] = None, del_local_after_load: int = True, **kwargs
@@ -463,6 +546,7 @@ class TorchTitanEngine(BaseEngine):
         if self._is_offload_param:
             for module in self.module:
                 load_fsdp_model_to_gpu(module)
+            self._refresh_checkpoint_cache()

         # Override TorchTitan's folder to use verl's path
         parent_dir = os.path.dirname(local_path)
@@ -480,10 +564,14 @@ class TorchTitanEngine(BaseEngine):
         torch.distributed.barrier()
         if self._is_offload_param:
             for module in self.module:
-                offload_fsdp_model_to_cpu(module)
+                offload_fsdp_model_to_cpu(module, empty_cache=False)
+            self._invalidate_checkpoint_cache()
+            aggressive_empty_cache(force_sync=True)
+            self._assert_checkpoint_cache_released()

         if self._is_offload_optimizer:
-            offload_fsdp_optimizer(self.optimizer)
+            for optimizer_instance in self.optimizer.optimizers:
+                offload_fsdp_optimizer(optimizer_instance)

     def get_per_tensor_param(self, **kwargs):
         for module in self.module:
@@ -596,7 +684,7 @@ class TorchTitanEngineWithLMHead(TorchTitanEngine):
                 position_ids = position_ids.values().unsqueeze(0)

             labels = torch.roll(input_ids, shifts=-1, dims=1)
-            attn_type = self.trainer.model_config.layer.attention.attn_backend
+            attn_type = self.engine_config.attn_type
             attention_mask = get_attention_masks(
                 input_batch=input_ids,
                 positions=position_ids,
diff --git a/verl/workers/engine/torchtitan/utils.py b/verl/workers/engine/torchtitan/utils.py
index a88c82e..0b3c9d3 100644
--- a/verl/workers/engine/torchtitan/utils.py
+++ b/verl/workers/engine/torchtitan/utils.py
@@ -123,10 +123,11 @@ def derive_torchtitan_name_and_flavor(hf_config) -> tuple[str, str]:
     num_layers = hf_config.num_hidden_layers
     vocab_size = hf_config.vocab_size

-    for flavor_name, model_cfg in model_configs.items():
+    for flavor_name, model_factory in model_configs.items():
+        model_cfg = model_factory()
         if (
             getattr(model_cfg, "dim", None) == hidden_size
-            and getattr(model_cfg, "n_layers", None) == num_layers
+            and len(getattr(model_cfg, "layers", []) or []) == num_layers
             and getattr(model_cfg, "vocab_size", None) == vocab_size
         ):
             logger.info(
diff --git a/verl/workers/engine_workers.py b/verl/workers/engine_workers.py
index 8784d3a..df88fb2 100644
--- a/verl/workers/engine_workers.py
+++ b/verl/workers/engine_workers.py
@@ -405,7 +405,7 @@ class TrainingWorker(Worker, DistProfilerExtension):
             ).cpu()
         else:
             final_output = None
-
+        aggressive_empty_cache(force_sync=True)
         return final_output

     @register(dispatch_mode=Dispatch.ONE_TO_ALL)
