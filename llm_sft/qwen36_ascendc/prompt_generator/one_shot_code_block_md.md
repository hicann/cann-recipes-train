## 示例代码（仅作格式模板，不是你要生成的算子）

样例算子 `{{example_name}}` 代码演示 4 个代码交付件的结构，**看结构、勿抄语义**。

以下是样例算子 `{{example_name}}` 的 4 个代码交付件：

kernel_src

```cpp
{{kernel_src}}
```

launch_h_src

```cpp
{{launch_h_src}}
```

plugin_src

```cpp
{{plugin_src}}
```

cmake_src

```cmake
{{cmake_src}}
```
